name: Weekly Insider Deals

on:
  schedule:
    - cron: '0 3 * * 0'
  workflow_dispatch:

jobs:
  update-insider-deals:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - name: Skip - insider_deal API chua duoc ho tro boi vnstock
        run: |
          echo "insider_deal() tra ve NotImplementedError tren tat ca 811 symbols"
          echo "Day la loi cua vnstock server, khong phai code"
          echo "Workflow nay se duoc kich hoat lai khi vnstock cap nhat API"
          exit 0
